{
  "name": "prediction-market-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Deployable serverless proxy for prediction-market data APIs.",
  "scripts": {
    "dev:netlify": "netlify dev",
    "deploy:netlify": "netlify deploy --prod --dir=public --functions=netlify/functions",
    "deploy:vercel": "vercel --prod",
    "test": "node tests/proxy-core.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "netlify-cli": "^26.0.0",
    "vercel": "^46.1.1"
  }
}
